import React from 'react';
import './Header.css'
import Hero from '../Hero/Hero';

const Header = () => {
    return (
        <section className="header">
            <Hero/>
        </section>
    );
};

export default Header;